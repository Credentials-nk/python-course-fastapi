[project]
name = "python-course-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cowpy>=1.1.5",
    "fastapi[standard]>=0.129.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
]

[tool.ruff.lint]
select = ["E", "F"]
